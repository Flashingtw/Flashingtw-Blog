---
import { t } from "@/i18n";

interface Props {
  answer?: string;
}

const { answer = "" }: Props = Astro.props;
---

<span
  class="quiz-gap"
  data-answer={answer}
  aria-label={t("accessibility.quizGap")}
>
  {answer}
</span>
