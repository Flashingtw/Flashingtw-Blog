---
import { t } from "@/i18n";

interface Props {
  defaultValue?: string;
  class?: string;
}

const { defaultValue = "", class: className = "" }: Props = Astro.props;
---

<div class={`tabs ${className}`.trim()} data-default-value={defaultValue}>
  <div class="nav" aria-label={t("accessibility.tabs")}>
    <ul role="tablist"></ul>
  </div>

  <div class="tabs-panels">
    <slot />
  </div>
</div>
